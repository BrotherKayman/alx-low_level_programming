#include "main.h"
/**
 * print_last_digit - Prints last digit
 * @r: checks last digit
 * Return: the value of the last digit
 */
int print_last_digit(int r)
{
int n;
if (r >= 0)
{
n = r % 10;
}
else
{
  n = ((r % 10) * -1);
}
_putchar('0' + r);
return(n);
}
