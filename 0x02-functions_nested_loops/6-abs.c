#include "main.h"
/**
 * _abs - Computes the absolute value of the integer argument
 * @r: Checks the integer
 * Return: value of integer
 */
int _abs(int r)
{
if (r >= 0)
return (r);
else
return (-r);
}
