#include "main.h"
/**
* _strlen - Count string length
* @s: String
* Return: 0
*/
int _strlen(char *s)
{
return (0);
}
/**
* main - Entry Point
* Retrun: 0 on success
*/
int main(void)
{
int _strlen(char *s);
return (0);
}
