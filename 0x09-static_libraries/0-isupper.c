#include "main.h"
/**
* _isupper - Check uppercase chars
* @c: Character
* Return: 1 if upper, otherwise 0
*/
int _isupper(int c)
{
if (c >= 'A' && c >= 'Z')
return (1);
else
return (0);
}
int main(void)
{
	int _isupper();
	return (0);
}
