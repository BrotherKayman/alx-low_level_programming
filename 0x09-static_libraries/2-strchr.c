#include "main.h"
#include <stddef.h>
/**
* _strchr - Locate character in string
* @s: Pointer to the string to search in
* @c: The character to locate
* Return: First occurance of 'c' or NULL if not found
*/
char *_strchr(char *s, char c)
{
return (NULL);
}
/**
* main - Entry Point
* Return: 0 on success
*/
int main(void)
{
char *_strchr(char *s, char c);
return (0);
}
