#include "main.h"
#include <stddef.h>
/**
* _strpbrk - Search a string for any of a set of bytes
* @s: Pointer to string to search in
* @accept: Pointer to string to search
* Return: A pointer to first occurrence in 's',or NULL if not found
*/
char *_strpbrk(char *s, char *accept)
{
return (NULL);
}
/**
* main - Entry Point
* Return: 0 on success
*/
int main(void)
{
char *_strpbrk(char *s, char *accept);
return (0);
}
