#include <stdio.h>
/**
 * main -  Program prints all single digit numbers of base 10 starting from 0
 * Return 0: on success
 */
int main(void)
{
  int num;
  for (num = 48; num <= 57; num++)
    {
      putchar(num);
    }
  putchar('\n');
  return (0);
}
