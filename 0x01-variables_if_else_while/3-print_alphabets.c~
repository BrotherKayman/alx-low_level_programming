#include <stdio.h>
/**
 * main - Program will print a - z both lower & uppercase
 * Return: 0 on success
 */
int main(void)
{
int lower = 97;
int upper = 65;
while (lower <= 122)
{
putchar(lower);
lower++;
}
while (upper <= 90)
{
putchr(upper);
upper++;
}
putchar('\n');
return (0);
}
